name: ResumeLetterAI CI/CD

on:
  push:
    branches:
      - '**' # সব branch এ push হলে run করবে

jobs:
  # -----------------------------
  # Job 1: Run Tests
  # -----------------------------
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run tests (skip untested files)
        run: |
          if [ -d "__tests__" ]; then
            echo "✅ Running tests for ResumeLetterAI..."
            yarn test
          else
            echo "⚠️ No tests found in ResumeLetterAI, skipping..."
          fi

  # -----------------------------
  # Job 2: Build (depends on tests)
  # -----------------------------
  build:
    name: Build ResumeLetterAI
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build project
        run: yarn build

  # -----------------------------
  # Deploy jobs are commented out for future use
  # -----------------------------
#  deploy-tested:
#    name: Deploy Tested Code (ResumeLetterAI)
#    runs-on: ubuntu-latest
#    needs: build
#    if: success()
#    steps:
#      - name: Deploy tested code
#        run: echo "🚀 Deploying tested ResumeLetterAI code to production..."
#
#  deploy-untested:
#    name: Deploy Untested Code (ResumeLetterAI)
#    runs-on: ubuntu-latest
#    needs: build
#    if: always()
#    steps:
#      - name: Deploy untested code
#        run: echo "⚠️ Deploying untested ResumeLetterAI code (be careful)..."
